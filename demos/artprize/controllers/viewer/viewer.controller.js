/*globals app*/
/*eslint no-console: ["error", { allow: ["info", "error"] }]*/
app.controller(`blockforgeViewer`, [`$scope`, `constants`, function(){
  console.info(`Viewer Controller Ready...`);
}]);